import { db } from "../models";
import { QueryTypes } from 'sequelize';
import briefInformationDTO from "../interface/res/briefInformationDTO";
import previewDTO from "../interface/res/previewDTO";


export async function previewTodayService(userId: string){

}

export async function previewThemeService(theme: string, userId: string){

}

export async function previewLocalService(local: string, userId: string){

}